import React from 'react'

type Props = {}

const ClientSide = (props: Props) => {
  return (
    <div>
      <h1>Client Side Rendering</h1>
      <p>This is a simple example of client-side rendering.</p>
      <p>By default, React components are rendered on the client side.</p>
    </div>
  )
}

export default ClientSide